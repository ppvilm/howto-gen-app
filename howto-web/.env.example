VITE_GRAPHQL_URL=http://localhost:4000/graphql
VITE_GRAPHQL_WS_URL=ws://localhost:4000/graphql
# Optional: base for file links like /files (used for screenshots, videos, markdown)
# If not set, UI derives base from VITE_GRAPHQL_URL or VITE_GRAPHQL_WS_URL
# VITE_SERVER_BASE_URL=http://localhost:4000
