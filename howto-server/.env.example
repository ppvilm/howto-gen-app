# Database connection (Docker Compose default)
DATABASE_URL=postgresql://howto:howto@localhost:8899/howto?schema=public

# App secrets (change in production)
JWT_SECRET=dev-secret-change-me

# Legacy absolute workspace path (optional; discouraged)
# HOWTO_WORKSPACE_DEFAULT=/absolute/path/.howto

# Preferred: server-local storage root. Actual workspace path will be
#   ${HOWTO_STORAGE_ROOT}/<accountId>/<workspaceId>
# Default (if unset): howto-server/storage
# HOWTO_STORAGE_ROOT=/var/lib/howto

# Server
PORT=4000

# Optional: seed user credentials for `npm run prisma:seed`
# SEED_USER_EMAIL=demo@example.com
# SEED_USER_PASSWORD=demo1234
